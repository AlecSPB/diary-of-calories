<3 новые возможности:
	<3 история в виде графика с поддержкой масштабирования:
		<3 отладить мультитач-масштабирование (двумя пальцами);
		<3 сделать возможность масштабирования на устройствах без поддержки
			мультитача (одним пальцем - удержание и перемещение);
		<3 добавить возможность сброса смещения и масштаба к первоначальному
			состоянию (по двойному клику);
	<3 сделать учёт пропущенных дней - если в течении нескольких дней программой
		не пользовались, она должна считать за эти дни нулевые калории, а не
		пропускать их (в списке и на графике; нулевые данные нет необходимости
		добавлять в БД, достаточно просто добавлять их в список и график);
	<3 сделать экспорт и импорт истории в виде файла (экспорт с возможностью
		выбора имени экспортного файла и пути его сохранения, импорт с
		возможностью выбора эспортного файла из файловой системы);
	<3 сделать возможность вычитать очередную запись (добавить знак к записям в
		БД, учитываемый при подсчёте суммы за день);
	<3 как вариант предыдующего пункта, сделать возможность записывать выражение
		для каждой записи, то есть и знак, и произведение, и деление;
	<3 сделать возможность добавления комментариев к записям в БД;
	<3 сделать возможность просмотра всех записей из БД в списке (с
		комментариями), а не только суммы за день;
	<3 сделать поддержку двух границ;
<3 привести приложение в соответствие с гайдами по дизайну Андроида;
<3 стиль и оформление кода:
	<3 собирать строки запросов к БД с помощью String.format();
	<3 по-максимуму вынести строки запросов к БД в классе DataAccessor в
		константы;
	<3 решить с нужностью разделения классов ветора и размера в зависимости от
		типа чисел;
	<3 выделить из класса Utils методы преобразования формата в отдельный класс
		FormatConverter;
